Cabinet portraits are sourced from Wikimedia Commons (public domain / CC-licensed
government and press portraits). Filenames map to members in `src/lib/seed/cabinet.ts`.

When adding a new minister, drop a ~480px-wide JPEG at `/cabinet/<slug>.jpg` and set
`photo: "/cabinet/<slug>.jpg"` on that member.
